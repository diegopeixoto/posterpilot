## 1. Capture

- [x] 1.1 Shot list + conventions (defined here): **dark theme**, ~1280px viewport, a representative populated library. Shots: dashboard, library grid, item detail / apply, Settings (incl. Security), Kometa manager, first-run wizard.
- [x] 1.2 Capture and optimize the approved images as WebP; store the shared set under the docs static assets path and the README highlights under `artwork/screenshots/`.

## 2. Embed

- [x] 2.1 Store optimized WebP assets under the docs static assets path and embed them at the relevant points in `installation.md`, `configuration.md`, and `usage.md` with descriptive alt text.
- [x] 2.2 Confirm Starlight bundles the assets and links resolve.

## 3. Verification

- [x] 3.1 Build the docs site; verify images render.

---

**Status:** implementation complete; all captures are bundled and verified by the docs build.
